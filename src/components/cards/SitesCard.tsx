import React from 'react'

const SitesCard: React.FunctionComponent<any> = ({text}) => {
    return (
        <div className="title">
            {text}
        </div>
    );
};

export default SitesCard;
